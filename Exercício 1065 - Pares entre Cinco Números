#include <stdio.h>
int main()
{
    int a, b, x=0;
    for(a=0; a<5; a++)
    {
        scanf("%d", &b);
        if(b%2==0)
        {
            x++;
        }
    }
    printf("%d valores pares\n", x);
    return 0;
}
